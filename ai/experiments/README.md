# Experiments

Exploratory notebooks and one-off training-run logs go here — never
imported by application code (`main.py`, `routers/`, `services/`), per the
Phase 3A design document, Step 4.1. Gitignored except this README and
`.gitkeep`, same convention as `backend/logs/`.
